"""W3C multi-chain SLA test package.

Entry points (run from repo root):

    python -m w3c.report     # run the multi-chain SLA test
    python -m w3c.discover   # build the per-chain address pool
"""
